<resources>

    <!-- Base application theme. -->
    <style name="Base.Theme.Friendship_Clinic" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorAccentMain</item>
        <item name="android:windowBackground">@color/white</item>
        <item name="android:colorBackground">@color/white</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">false</item>
    </style>

    <style name="Toolbar.TitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">14sp</item>
    </style>

    <style name="spinnerTheme" parent="Base.Widget.AppCompat.Spinner">
        <item name="android:textColor">@color/gray_7</item>
        <item name="android:textSize">14sp</item>
    </style>
</resources>